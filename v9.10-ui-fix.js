/* V10 compatibility stub.
   The old V9.10 UI wrapper is intentionally disabled.
   No render hooks, MutationObserver, duplicate-card cleanup, or script loading.
*/
(function(){
  'use strict';
  window.__v910UiFixDisabled=true;
})();
